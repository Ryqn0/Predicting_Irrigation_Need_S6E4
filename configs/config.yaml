data:
  raw_path: data/raw/
  processed_path: data/processed/

model:
  name: xgboost
  params:
    n_estimators: 300
    max_depth: 6
    learning_rate: 0.05

training:
  test_size: 0.2
  random_state: 42
  cv_folds: 5

mlflow:
  experiment_name: irrigation-need
  tracking_uri: mlruns/
